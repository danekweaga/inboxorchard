# Community automation templates

These files use the same schema as Inbox Orchard's visual builder and API. Import a definition through `POST /api/automations`, or use it as a starting point for a community template.

Before publishing, replace values beginning with `SELECT_` with IDs from your own installation. Always run validation and the simulator after importing a template. Template files contain no credentials and never execute external actions merely by being imported.
